import { PosicionDirective } from './posicion.directive';

describe('PosicionDirective', () => {
  it('should create an instance', () => {
    const directive = new PosicionDirective();
    expect(directive).toBeTruthy();
  });
});
