import { AgrandaDirective } from './agranda.directive';

describe('AgrandaDirective', () => {
  it('should create an instance', () => {
    const directive = new AgrandaDirective();
    expect(directive).toBeTruthy();
  });
});
